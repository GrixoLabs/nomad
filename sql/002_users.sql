-- DEPRECATED: early profile-only users table.
-- Prefer sql/003_auth_users.sql for production auth (UUID users + OTP + JWT).
-- Kept only for reference; do not run this on new environments.

-- See 003_auth_users.sql
